# 小说管理器 v3.2.0 联动改进计划

> 基线：commit `2376709`（数据模块页面化 + 全局快捷入口）
> 日期：2026-08-02
> 状态：待评审

## 一、Bug 修复（控制台报错）

### 1.1 `渲染属性失败: Cannot set properties of null (setting 'innerHTML')`

- **位置**：[app.js:203](file:///g:/code/novel_manager_v3.1.0/desktop/www/static/js/app.js#L203) `renderStats()` 调用
- **原因**：`renderStats()` 内部尝试写入某个 DOM 元素的 innerHTML，但该元素在当前页面下不存在（例如 `#stats-info` 仅在「数据预览」静态页存在，初始化时不在 DOM 中）
- **修复**：在 `renderStats()` 内部加 `const el = document.getElementById('xxx'); if (!el) return;` 守卫，或在 `loadData()` 调用处判断当前页面再渲染

### 1.2 `[ModuleRegistry] 搜索失败 (inventory/quests/skills): ad.xxx.forEach is not a function`

- **位置**：[legacy_modules_register.js:224/237/250](file:///g:/code/novel_manager_v3.1.0/desktop/www/static/js/legacy_modules_register.js#L220-L260) `searchInventory` / `searchQuests` / `searchSkills`
- **原因**：searchIndexer 函数假设 `ad.inventory` / `ad.quests` / `ad.skills` 是数组，但实际存储为对象（`{id: {...}}`）
- **修复**：在每个 searchXxx 函数入口做归一化：
  ```js
  const list = Array.isArray(ad.inventory) ? ad.inventory : Object.values(ad.inventory || {});
  list.forEach(item => { ... });
  ```
- **影响模块**：inventory / quests / skills / worldview（worldview 的 entries.forEach 同样问题）

### 1.3 `TypeError: modules.forEach is not a function` (mod_data_import.js:90)

- **位置**：[mod_data_import.js:90](file:///g:/code/novel_manager_v3.1.0/desktop/www/static/js/modules/mod_data_import.js#L85-L95) `populateTargetModules`
- **原因**：`ModuleRegistry.getAllModules()` 返回对象（`{id: mod}`）而非数组
- **修复**：改为 `Object.values(modules).forEach(m => { ... })`

### 1.4 修复优先级

| 优先级 | Bug | 影响 |
|--------|-----|------|
| P0 | 1.2 searchIndexer 归一化 | 全文搜索功能完全不可用 |
| P0 | 1.3 modules.forEach | 数据导入下拉框为空 |
| P1 | 1.1 renderStats 空指针 | 控制台噪音，不影响功能 |

---

## 二、术语表与其他模块联动

### 2.1 现状

- 术语表（glossary）是独立的数组模块 `[{id, term, definition, category, ...}]`
- 章节正文审查（chapter_review）已读取 glossary 数据用于查重，但只是只读
- 角色信息 / 世界观 / 时间线等模块与术语表无任何关联

### 2.2 联动设计

#### A. 术语自动提取
- 在「章节管理」编辑器中增加「提取术语」按钮，扫描当前章节正文，自动识别未在术语表中的新名词
- 识别策略：基于词频 + 词性启发（中文 2-6 字、不含常用词），可结合 AI 调用 `extractTerms` 工具
- 提取结果以列表展示，用户勾选后批量加入术语表

#### B. 术语高亮与悬浮卡
- 在「章节正文审查」的内容预览区，对正文中的术语自动加下划线/高亮
- 鼠标悬浮术语时弹出卡片显示定义（从 glossary 实时读取）
- 点击卡片「查看详情」可跳转到术语表对应条目

#### C. 术语反向链接
- 术语表条目详情页显示「出现在哪些章节」列表（扫描全部章节正文）
- 点击章节链接跳转到对应章节编辑器并定位术语位置

#### D. 角色与世界观的术语关联
- 角色信息编辑表单增加「关联术语」字段（多选下拉，从 glossary 读取）
- 世界观条目增加「关联术语」字段
- 角色详情/世界观详情页显示关联术语列表，点击可查看定义

### 2.3 实施步骤

1. 在 glossary 模块增加 `extractFromText(text)` 工具函数（纯前端词频统计）
2. 在 mod_chapters.js 编辑器工具栏增加「提取术语」按钮
3. 在 mod_chapter_review.js 渲染正文时调用 `highlightTerms(text, glossary)`
4. 在 mod_glossary.js 增加条目详情 modal，显示反向链接
5. 在 character/worldview 表单增加术语关联字段

---

## 三、章节正文审查 - 内容预览

### 3.1 现状

- [mod_chapter_review.js](file:///g:/code/novel_manager_v3.1.0/desktop/www/static/js/modules/mod_chapter_review.js) 选择章节后只显示操作按钮（重写/扩写/精简/续写/查设定），无章节正文预览
- 用户需先到「章节管理」查看正文，再切回审查模块操作，体验割裂

### 3.2 改进方案

#### A. 双栏布局
- 选择章节后，左侧显示章节正文（只读 + 行号），右侧显示审查结果/AI 输出
- 正文区支持选中文字后弹出悬浮菜单（已有 `.ai-float-menu`，复用即可）
- 正文区顶部显示章节标题、字数、状态徽章

#### B. 选中即操作
- 选中正文片段后，悬浮菜单自动出现，按钮变为针对选中内容的操作
- 「查设定」按钮对选中文字调用 glossary/worldview/character 联合查询，结果在右侧面板显示
- 「重写/扩写/精简」对选中内容调用 AI，结果在右侧面板显示对比（原文 vs 新文）

#### C. 一键应用
- 右侧 AI 输出区有「替换选中」「插入到选中后」「追加到章节末尾」三个按钮
- 点击「替换选中」直接修改章节正文并持久化（调用 `/api/mod/chapters/save`）

### 3.3 实施步骤

1. 重构 `renderPage()` 为双栏 grid 布局
2. 增加 `renderChapterPreview(chapterId)` 渲染左侧正文
3. 修改悬浮菜单逻辑：选中文字 → 显示菜单 → 调用 AI → 右侧显示结果
4. 增加「应用结果」按钮组，调用 `applyAiResult(mode, result)`

---

## 四、章节管理 - 编辑器窗口化

### 4.1 现状

- [mod_chapters.js](file:///g:/code/novel_manager_v3.1.0/desktop/www/static/js/modules/mod_chapters.js) 点击「编辑」后展开一个全屏覆盖的编辑器
- 编辑器占用整个视口，无法同时查看其他模块（如术语表、世界观）

### 4.2 改进方案

#### 方案对比

| 方案 | 优点 | 缺点 |
|------|------|------|
| A. 模态窗口（居中 + 半透明遮罩） | 视觉聚焦，符合习惯 | 遮罩挡住其他内容 |
| B. 右侧滑入面板（与快捷面板类似） | 可同时看左侧导航 + 主内容 | 宽度受限，长文编辑局促 |
| C. 分屏（左 60% 列表 + 右 40% 编辑器） | 并排查看，无需切换 | 需重构页面布局 |
| D. 可拖拽浮窗（自由调整位置/大小） | 最灵活 | 实现复杂，需引入或自写拖拽逻辑 |

**推荐方案 C（分屏）**：
- 章节列表保留在左侧（占 40%），编辑器在右侧（占 60%）
- 编辑器顶部 tab 栏可切换「大纲 / 正文 / 审查」三视图
- 点击列表中其他章节即时切换编辑器内容，无需关闭重开
- 移动端折叠为单栏（列表 → 编辑器栈式导航）

### 4.3 实施步骤

1. 重构 `renderPage()` 为 `display:grid; grid-template-columns: 40% 60%`
2. 编辑器从全屏覆盖改为右侧固定区域
3. 列表项点击切换编辑器内容（保留未保存改动提示）
4. 增加「全屏编辑」按钮作为可选切换（保留原全屏模式作为备选）

---

## 五、对话栏快捷按钮 - 改造

### 5.1 现状问题

- [ai_chat.js](file:///g:/code/novel_manager_v3.1.0/desktop/www/static/js/ai_chat.js#L185-L218) 我新增的快捷按钮区存在以下问题：
  1. 按钮过多导致横向溢出，部分被截断不可见
  2. 导航类按钮（章节/角色/背包等）点击后直接 `switchPage` 跳转，打断写作流
  3. 「分析状态」按钮只是预填输入框，无实际联动逻辑
  4. 缺少「将选中内容导入对话框」的能力

### 5.2 改进方案

#### A. 按钮分组与折叠
- 将 9 个按钮重组为 3 类：
  - **常用**：角色概要、分析状态（2 个，常驻显示）
  - **跳转**：章节/角色/世界观/术语表（4 个，点击展开为下拉）
  - **数据**：背包/货币/技能/任务（4 个，点击展开为下拉）
- 常用按钮直接显示，其余收纳到「更多 ▼」下拉中

#### B. 改为弹出内容面板（而非跳转）
- 点击「角色」按钮 → 不跳转，而是在对话栏上方弹出小型角色概要浮层（200px 高）
- 浮层显示角色核心字段 + 「展开完整页面」链接（点击才真正 switchPage）
- 浮层内容支持选中文字 → 拖拽或点击「插入到对话框」按钮，将选中文字填入 `#ai-chat-input`

#### C. 内容导入对话框
- 在每个模块页面（货币/背包/技能等）的列表项增加「加入对话」按钮
- 点击后将该条目格式化为文本（如「🪙 金币 ×100」）追加到 `#ai-chat-input`
- 支持「加入正文」按钮，将条目格式化后插入当前编辑中的章节正文光标位置

#### D. 「分析状态」按钮增强
- 点击后不只是预填提示词，而是：
  1. 自动收集当前角色/货币/背包/技能/任务快照
  2. 构造结构化 prompt：「当前主角：xxx，持有：xxx，技能：xxx，未完成任务：xxx，请分析并给出下一步建议」
  3. 直接调用 `AiChatBar.send(prompt)` 发送，无需用户手动按回车

### 5.3 实施步骤

1. 重构 `#ai-chat-quick-row` 为分组 + 下拉结构
2. 新增 `showQuickPanel(moduleId)` 函数，在对话栏上方显示小型内容浮层
3. 在各数据模块列表项增加「加入对话」「加入正文」按钮（可通过 ModuleRegistry 注入统一渲染钩子）
4. 增强「分析状态」按钮：收集快照 → 构造 prompt → 自动发送

---

## 六、FAB 悬浮按钮 - 改造

### 6.1 现状问题

- [quick_access.js](file:///g:/code/novel_manager_v3.1.0/desktop/www/static/js/quick_access.js) 当前 FAB 菜单点击模块项直接 `switchPage` 跳转
- 用户期望：FAB 应该是「快捷查看/操作面板」，而不是第二个导航栏
- 右侧加号点击后跳转页面，打断了当前写作上下文

### 6.2 改进方案

#### A. FAB 菜单改为「内容预览卡」
- 点击 FAB 后，不弹出导航菜单，而是弹出「快速操作面板」：
  - 顶部：当前角色快照（名称/等级/关键属性）
  - 中部：tab 切换（货币/背包/技能/任务），每个 tab 显示该模块 top 5 条目
  - 底部：操作按钮（「展开完整页面」跳转 / 「加入对话」/ 「关闭」）

#### B. 长按 FAB = 导航菜单
- 短按 FAB → 弹出内容预览卡（默认行为）
- 长按 FAB（>500ms）→ 弹出导航菜单（当前行为）
- 区分两种交互，避免误跳转

#### C. 模块内直接增删
- 在内容预览卡中，每个条目右侧显示「编辑」「删除」小按钮
- 点击「编辑」弹出 modal 表单（复用各模块已有的 showAddXxx 逻辑）
- 点击「+」按钮在各模块内新增条目
- 这样用户在写作时无需切换页面即可管理数据

#### D. 自定义内容选择
- 内容预览卡中每条目前增加 checkbox
- 底部增加「导出选中到对话」「导出选中到正文」按钮
- 导出格式可配置（紧凑/详细/Markdown 表格）

### 6.3 实施步骤

1. 重构 `buildMenu()` 为 `buildQuickPanel()`，内容来自实时数据
2. 增加 tab 切换逻辑，复用 `apiRequest('/api/mod/{name}')`
3. 集成各模块的 showAddXxx/showEditXxx 函数（需暴露到 window）
4. 增加批量选择与导出逻辑
5. 长按检测：`mousedown` 记录时间，`mouseup` 时判断

---

## 七、跨模块内容导入流程

### 7.1 场景

用户在写作时希望快速将其他模块的数据插入到当前章节正文或 AI 对话中，例如：
- 写到主角买药时，快速把「灵草 ×5」从背包插入正文
- 写到战斗时，把「火球术 Lv3」技能描述插入对话问 AI 如何描写
- 写到任务进展时，把「寻找失落的剑」任务状态插入正文

### 7.2 统一导入接口

设计一个全局 `ContentImporter` 对象：

```js
window.ContentImporter = {
  // 将模块条目格式化为文本
  formatItem(moduleId, item, format = 'compact') -> string,
  // 插入到 AI 对话框
  toChat(text) -> void,
  // 插入到当前章节正文编辑器光标位置
  toChapter(text) -> void,
  // 弹出选择面板，多选条目
  showPicker(moduleId, { multiple, onConfirm }) -> void
};
```

### 7.3 各模块注册格式化器

在 ModuleRegistry.register 配置中增加 `itemFormatter` 字段：

```js
ModuleRegistry.register({
  id: 'inventory',
  // ...现有字段
  itemFormatter: (item, format) => {
    if (format === 'compact') return `${item.icon} ${item.name} ×${item.quantity}`;
    if (format === 'detailed') return `${item.name}（${item.description || ''}）×${item.quantity}`;
    return item.name;
  }
});
```

### 7.4 集成点

- 章节编辑器工具栏增加「插入数据」按钮 → 调用 `ContentImporter.showPicker()`
- AI 对话栏快捷按钮区增加「插入数据」按钮 → 调用 `ContentImporter.showPicker({ onConfirm: text => ContentImporter.toChat(text) })`
- FAB 快速面板每条目「→对话」「→正文」按钮 → 调用对应接口

---

## 八、实施优先级与路线图

### 阶段 1：Bug 修复 + 体验打磨（立即）
- [ ] 修复 6 个控制台报错（1.1 / 1.2 / 1.3）
- [ ] 对话栏快捷按钮分组折叠（5.2-A）
- [ ] FAB 按钮区分短按/长按（6.2-B）

### 阶段 2：内容预览与导入（短期）
- [ ] 章节审查双栏布局 + 正文预览（3.2-A/B/C）
- [ ] 章节编辑器分屏改造（4.2-C）
- [ ] ContentImporter 统一接口（7.2/7.3/7.4）
- [ ] 对话栏按钮改为弹出内容浮层（5.2-B/C）

### 阶段 3：FAB 改造 + 模块内 CRUD（中期）
- [ ] FAB 内容预览卡 + tab 切换（6.2-A）
- [ ] FAB 内直接增删改条目（6.2-C）
- [ ] 批量选择与导出（6.2-D）
- [ ] 「分析状态」按钮自动收集快照并发送（5.2-D）

### 阶段 4：术语表联动（中后期）
- [ ] 术语自动提取（2.2-A）
- [ ] 术语高亮与悬浮卡（2.2-B）
- [ ] 术语反向链接（2.2-C）
- [ ] 角色/世界观关联术语（2.2-D）

### 阶段 5：持续完善联动（长期）
- [ ] 章节字数实时同步到写作仪表盘
- [ ] 时间线事件从章节正文自动提取
- [ ] 角色关系图从章节对话自动更新
- [ ] 全模块全文搜索结果点击直接定位

---

## 九、设计原则

1. **不打断写作流**：任何快捷操作都不应强制切换页面，优先用浮层/面板/模态
2. **数据双向流动**：模块数据既能导入正文/对话，正文内容也能反向提取到模块
3. **统一交互模式**：所有模块的「加入对话」「加入正文」「编辑」「删除」按钮位置和行为保持一致
4. **渐进增强**：现有功能不破坏，新功能逐步叠加，用户可选择性启用
5. **性能优先**：浮层数据懒加载，避免初始化时全量拉取；长列表虚拟滚动
6. **键盘友好**：所有快捷操作都应有对应快捷键，Esc 统一关闭浮层

---

## 十、验收标准

每个阶段完成后需通过以下验证：

- [ ] 浏览器控制台无 error 级别日志
- [ ] 硬刷新（Ctrl+Shift+R）后所有功能正常
- [ ] 移动端窄屏（<600px）布局不破
- [ ] 暗色模式下文字/按钮可读
- [ ] 数据持久化：刷新后localStorage 数据不丢失
- [ ] 与 AI 工具调用协议（`<<tool:...>>`）兼容，不破坏现有 AI 工具
