# 创作工坊 V1.0 开发计划

> 版本号自 V1.0 开发版（v1.0.0-dev）重新起算。
> 本文档规划 V1.0 的目标、模块联动架构与分阶段实施路线。

---

## 1. 背景与问题

### 1.1 已确认的问题
| 问题 | 现状 | 处置 |
|---|---|---|
| 大纲功能嵌入章节管理，挤占编辑界面 | 已恢复双栏（列表 \| 编辑器），大纲独立为「大纲管理」模块 | ✅ 已解决 |
| 章节管理整页可整体上下滚动 | 已改为固定视口高度，仅各栏内部滚动 | ✅ 已解决 |
| 分屏滚动仅左→右单向同步 | 已实现编辑区 ↔ 预览区双向按比例联动 | ✅ 已解决 |
| 版本号长期未更新 | 已重置为 v1.0.0-dev | ✅ 已解决 |

### 1.2 核心痛点：模块之间缺乏联动
应用包含 30+ 模块，但多数相互独立、各自为政，写作时需要在多个页面间来回跳转、手动复制。典型断点：

- **章节正文** 中提到的角色 / 术语 / 时间点，无法点击跳转到对应设定页；
- **灵感** 素材无法一键插入章节正文，需要手动复制；
- **章节审查** 的 AI 审查结论（问题/建议）不会回写到章节待办或灵感库；
- **时间线** 与章节没有关联，章节时间点无法在时间线上体现；
- **全文搜索** 取数架构有缺陷（详见 §3.2），搜不到真实内容；
- **写作仪表盘** 与各模块只有部分字数联动，缺少目标/进度闭环。

---

## 2. V1.0 目标

1. **写作流闭环**：从大纲 → 素材（灵感/短语/设定）→ 正文 → 审查 → 修订，形成一条可连续操作的写作流水线，核心节点互相可跳转、可引用。
2. **设定互链**：角色 / 世界观 / 术语表 / 物品 / 地点等设定数据，在章节正文中可被识别、高亮并跳转（复用已有术语联动基础）。
3. **搜索可用**：全文搜索真正可搜到所有模块的真实内容（修复取数与字段规范）。
4. **UI 稳定**：所有主页面固定视口高度、栏内滚动，分屏/预览交互一致。
5. **质量基线**：统一转义、统一空态/复制/删除等公共交互，无控制台报错。

---

## 3. 模块联动架构

### 3.1 核心写作流水线

```
大纲管理 ──▶ 章节管理 ──▶ 章节审查 ──▶ 修订回写
   │             │            │
   ▼             ▼            ▼
 灵感库      角色/术语/世界观   修订记录 → 写作仪表盘
 短语库      时间线/地图       （字数/目标进度闭环）
```

- **大纲管理**（写作组）↔ **章节管理**：共用 chapters 数据，双向跳转（已实现）。
- **章节管理** ↔ **章节审查**：章节增删改后同步刷新审查列表（已实现）。
- **章节正文** ↔ **设定模块**：正文中识别角色/术语名 → 高亮 + 点击跳转（复用术语表反向链接机制）。
- **灵感/短语库** ↔ **章节正文**：ContentImporter 已提供跨模块导入，补齐「灵感 → 正文插入」入口。
- **章节审查** → **灵感/待办**：审查发现的问题可一键保存为灵感/修订待办。
- **时间线** ↔ **章节**：章节对象增加可选「时间点」字段，时间线模块按章节聚合展示。

### 3.2 必须重构的既有架构缺陷

| 项 | 问题 | 方案 |
|---|---|---|
| 全文搜索取数 | `searchIndexer(data, query)` 调用时未传 query、数据源 `window.appData` 为空且不含动态模块数据、返回字段（name/page）与展示字段（title/content/text）不匹配 | 统一调用 `searchIndexer(实际数据, query)`；数据源改为各模块 `dataKeys` 的真实数据；统一索引条目结构 `{name, type, page, content}` |
| 版本号/数据层 | 各文件头注释与 console 中的版本号分散 | 引入单一版本常量，集中输出 |

### 3.3 联动矩阵（目标态）

| 模块 A | 模块 B | 联动方式 | 阶段 |
|---|---|---|---|
| 大纲管理 | 章节管理 | 共用 chapters 数据 + 双向跳转 | P0 ✅ |
| 章节管理 | 章节审查 | 章节变更同步刷新 | P0 ✅ |
| 章节正文 | 角色/术语/世界观 | 正文术语高亮 + 点击跳转 | P1 |
| 灵感/短语库 | 章节正文 | 一键插入正文 | P1 |
| 章节审查 | 灵感库/待办 | 审查问题存为灵感/待办 | P2 |
| 时间线 | 章节 | 章节时间点聚合展示 | P2 |
| 写作仪表盘 | 章节/目标 | 字数 + 目标进度实时闭环 | P1 加强 |
| 全文搜索 | 全部模块 | 真实取数 + 统一字段 | P1 |
| 多项目 | 全部模块 | 项目级数据隔离 | P2 |
| 打印导出 | 全部模块 | 统一排版 + 转义修复 | P3 |

---

## 4. 分阶段实施路线

### P0 — 基础稳固（进行中，本次已交付）
- [x] 大纲独立模块，章节管理恢复双栏
- [x] 章节管理整页不滚动
- [x] 分屏双向滚动同步
- [x] 版本重置 v1.0.0-dev + 开发计划

### P1 — 联动打通（下一阶段）
- [ ] 全文搜索取数重构（§3.2），真正可搜到全部模块内容
- [ ] 正文术语/角色高亮 + 点击跳转设定页（复用 GlossaryModule 术语选择器）
- [ ] ContentImporter 强化：灵感/短语库 → 正文一键插入
- [ ] 写作仪表盘：今日目标进度与章节实时联动闭环
- [ ] 公共交互统一：空态文案 / copyText / confirmDelete / countWords 收归 UIUtils

### P2 — 写作流增强
- [ ] 章节审查 → 灵感/修订待办回写
- [ ] 时间线 ↔ 章节时间点聚合
- [ ] 多项目数据隔离完善
- [ ] AI 提取/审查与写作流水线打通（术语批量入库、审查问题汇总）
- [ ] 分屏/预览体验：字体、主题联动优化

### P3 — 质量与发布
- [ ] XSS 转义统一（print_export、renderIdBadge、v183 旧页面）
- [ ] 移动端副本（novel_manager_android）同步
- [ ] 控制台零报错基线 + 完整回归清单
- [ ] 版本规范：按 SemVer 从 1.0.0 起迭代，维护 CHANGELOG

---

## 5. 版本管理

- 当前版本：**v1.0.0-dev**（开发版）
- 版本号展示位置：侧边栏底部 / 设置页 / 版本历史 / CHANGELOG / tauri 配置 / 数据层 version
- 规则：P1 完成 → v1.1.0-dev；P2 完成 → v1.2.0-dev；达到发布标准 → v1.0.0 正式版
- 每次变更同步更新 CHANGELOG.md 与版本历史弹窗
